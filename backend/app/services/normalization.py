from __future__ import annotations

import pandas as pd


def normalize_prices(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for currency/unit normalization.

    Future: apply FX from CBR and normalize units (kg/liter/etc.).
    """
    return df
